import 'package:flutter/material.dart';

class ForthPage5UI extends StatelessWidget {
  const ForthPage5UI({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: Center(
        child: Text(
          'หึหึหึหึ -_-',
          style: TextStyle(
            fontSize: 50.0,
            fontWeight: FontWeight.bold,
            fontFamily: 'Kanit',
            color: Colors.blue[200],
          ),
        ),
      ),
    );
  }
}